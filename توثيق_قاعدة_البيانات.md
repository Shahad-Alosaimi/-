# توثيق قاعدة البيانات — تطمن 🎓
**جامعة الطائف | المسؤولة: العضوة الأولى**

---

## اسم قاعدة البيانات
```
tatman_db
```

---

## الجداول

### 1️⃣ جدول categories (التصنيفات)
| العمود | النوع | الوصف |
|--------|-------|-------|
| id | INT | رقم التصنيف |
| name_ar | VARCHAR | اسم التصنيف بالعربي |
| icon | VARCHAR | أيقونة FontAwesome |

**البيانات الجاهزة فيه:**
إلكترونيات، وثائق، محافظ، مفاتيح، مجوهرات، ملابس، أخرى

---

### 2️⃣ جدول users (المستخدمون)
| العمود | النوع | الوصف |
|--------|-------|-------|
| id | INT | رقم المستخدم |
| full_name | VARCHAR | الاسم الكامل |
| email | VARCHAR | البريد الجامعي (لا يتكرر) |
| student_id | VARCHAR | الرقم الجامعي |
| password_hash | VARCHAR | كلمة المرور مشفرة |
| profile_pic | VARCHAR | صورة الملف الشخصي |
| is_active | TINYINT | 1=نشط، 0=موقوف |
| created_at | DATETIME | تاريخ التسجيل |

---

### 3️⃣ جدول items (البلاغات)
| العمود | النوع | الوصف |
|--------|-------|-------|
| id | INT | رقم البلاغ |
| user_id | INT | صاحب البلاغ (من users) |
| category_id | INT | التصنيف (من categories) |
| type | ENUM | **lost** = مفقود / **found** = موجود |
| title | VARCHAR | عنوان البلاغ |
| description | TEXT | الوصف التفصيلي |
| building | VARCHAR | المبنى داخل الجامعة |
| location_desc | VARCHAR | وصف الموقع (قاعة، مدخل...) |
| incident_date | DATE | تاريخ الفقدان أو الإيجاد |
| image1 | VARCHAR | صورة رئيسية |
| image2 | VARCHAR | صورة ثانية (اختياري) |
| image3 | VARCHAR | صورة ثالثة (اختياري) |
| status | ENUM | **active** / **resolved** / **deleted** |
| views | INT | عدد المشاهدات |
| created_at | DATETIME | تاريخ النشر |

---

### 4️⃣ جدول messages (الرسائل)
| العمود | النوع | الوصف |
|--------|-------|-------|
| id | INT | رقم الرسالة |
| item_id | INT | البلاغ المرتبط |
| sender_id | INT | المرسل (من users) |
| receiver_id | INT | المستقبل (من users) |
| body | TEXT | نص الرسالة |
| is_read | TINYINT | 0=غير مقروءة، 1=مقروءة |
| created_at | DATETIME | وقت الإرسال |

---

### 5️⃣ جدول notifications (الإشعارات)
| العمود | النوع | الوصف |
|--------|-------|-------|
| id | INT | رقم الإشعار |
| user_id | INT | المستخدم المستقبل |
| type | VARCHAR | نوع الإشعار |
| message | VARCHAR | نص الإشعار |
| link | VARCHAR | رابط الصفحة |
| is_read | TINYINT | 0=غير مقروء، 1=مقروء |
| created_at | DATETIME | وقت الإشعار |

---

## تعليمات لكل عضوة 📌

### العضوة الثانية (header & footer & CSS)
```php
// في بداية كل صفحة حطي:
require_once 'config.php';
include 'includes/header.php';

// في نهاية كل صفحة:
include 'includes/footer.php';
```

### العضوة الثالثة (تسجيل الدخول)
```php
// تسجيل مستخدم جديد
INSERT INTO users (full_name, email, student_id, password_hash)
VALUES ('...', '...', '...', '...')

// تسجيل الدخول
SELECT * FROM users WHERE email='...' AND is_active=1
```

### العضوة الرابعة (إضافة البلاغات)
```php
// جلب التصنيفات
SELECT * FROM categories

// إضافة بلاغ
INSERT INTO items (user_id, category_id, type, title, description, building, location_desc, incident_date)
VALUES (...)

// type يقبل قيمتين فقط: 'lost' أو 'found'
// status الافتراضي: 'active'
```

### العضوة الخامسة والسادسة (عرض البلاغات والبحث)
```php
// جلب البلاغات مع بيانات المستخدم والتصنيف
SELECT items.*, users.full_name, categories.name_ar, categories.icon
FROM items
JOIN users ON items.user_id = users.id
JOIN categories ON items.category_id = categories.id
WHERE items.status = 'active'
ORDER BY items.created_at DESC

// البحث
WHERE items.title LIKE '%كلمة البحث%'
AND items.type = 'lost' -- أو 'found'
AND items.building LIKE '%المبنى%'
```

### العضوة السابعة (لوحة التحكم)
```php
// تعديل حالة البلاغ
UPDATE items SET status='resolved' WHERE id=? AND user_id=?

// حذف البلاغ (مخفي مو محذوف فعلاً)
UPDATE items SET status='deleted' WHERE id=? AND user_id=?
```

### العضوة الثامنة (الرسائل)
```php
// إرسال رسالة
INSERT INTO messages (item_id, sender_id, receiver_id, body)
VALUES (...)

// جلب الرسائل غير المقروءة
SELECT COUNT(*) FROM messages
WHERE receiver_id=? AND is_read=0

// تعليم كمقروءة
UPDATE messages SET is_read=1 WHERE item_id=? AND receiver_id=?
```

### العضوة التاسعة (الأمان)
```php
// دالة التنظيف موجودة في config.php
$input = clean($conn, $_POST['field']);

// التحقق من تسجيل الدخول
if (!isLoggedIn()) redirect('login.php');
```

---

## ملاحظة مهمة ⚠️
- لا يوجد حقل **مدينة** — الموقع بس للجامعة
- استخدمي `building` للمبنى و `location_desc` للموقع التفصيلي
- البريد الإلكتروني يجب أن يكون **جامعياً**
